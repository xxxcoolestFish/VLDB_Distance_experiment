# 实验1：策略一 — 解耦投影头 (Disentangled Projection Heads)

**日期**: 2026-06-11
**分支**: `exp1_disentangled_pj`
**代码**: `exp1_models/rne_l1tilde.py`, `exp1_models/rgnndist2vec_l1tilde.py`

---

## 一、实验动机

### 问题

当前 L̃₁ 实现（`models/rne_l1tilde.py`, `models/rgnndist2vec_l1tilde.py`）使用**硬切片**：

```python
# 原版: 同一层输出的不同切片
sym = |emb[:, :62] − emb[:, :62]|.sum()   # V 型梯度
asym = (emb[:, 62:] − emb[:, 62:]).sum()  # 线性无界梯度
```

GNN / Embedding 的最后一层同时输出两种梯度特性完全不同的度量，导致：
- **RNE 在大图上爆炸**（26% → 41.84%）
- **梯度冲突**，优化困难

### 改进方案

```python
# 改进: 独立解耦投影
h = encoder(x)                        # 通用特征
h_sym = proj_sym(h)                   # nn.Linear(d, r) 独立参数
h_asym = proj_asym(h)                 # nn.Linear(d, s) 小权重初始化
sym_dist = |v_sym − u_sym|.sum()
asym_dist = (v_asym − u_asym).sum()
distance = clamp(sym + asym, 0) * max_distance
```

- `proj_sym`: 专门学习对称特征
- `proj_asym`: 专门学习方向特征，初始化 `std=0.01` → 粗到细学习

---

## 二、实验设置

- **数据集**: Harbin (44K 节点)，其余城市暂不跑
- **训练**: 20 epoch, batch_size=1024, device=cuda
- **RNE**: lr=0.003, loss=mse, embed_dim=64
- **RGNN**: lr=0.01, loss=smoothl1, embed_dim=64
- **L̃₁**: r=62, s=2

### 模型对照表

| model_class | 来源 | 说明 |
|------|------|------|
| `rne` | `models/rne.py` | RNE L1-mean（原始） |
| `rne_l1tilde` | `models/rne_l1tilde.py` | RNE 硬切片 L̃₁（现有） |
| `rne_l1tilde_proj` | `exp1_models/rne_l1tilde.py` | RNE 解耦投影 L̃₁（**新**） |
| `rgnndist2vec` | `models/rgnndist2vec.py` | GNN L1（原始） |
| `rgnndist2vec_l1tilde` | `models/rgnndist2vec_l1tilde.py` | GNN 硬切片 L̃₁（现有） |
| `rgnndist2vec_l1tilde_proj` | `exp1_models/rgnndist2vec_l1tilde.py` | GNN 解耦投影 L̃₁（**新**） |

---

## 三、实验结果

### Harbin Test MRE 汇总

| 模型 | L1 原版 | L̃₁ 硬切片 | L̃₁ 解耦投影 | Δ (proj vs hard) |
|------|:---:|:---:|:---:|:---:|
| **RNE** | 26.11% | 41.84% 💥 | **22.27%** ✅ | **-19.57pp** 🔥 |
| **RSAGE** | 6.51% | 6.56% | 7.00% | +0.44pp |
| **RGAT** | 5.88% | 5.49% | **4.96%** ✅ | **-0.53pp** |

> L1 原版和硬切片 L̃₁ 数据来自 `第一次消融分析.md`。

### RNE 详细 (22.27%)

| Bucket | 距离范围 | MRE | 占比 |
|:---:|------|:---:|:---:|
| 1 | 0–0 (最短) | 38.79% | 49.41% |
| 2 | 0–0 | 6.78% | 31.22% |
| 3 | 0–1 | 5.29% | 15.00% |
| 4 | 1–1 | 4.31% | 4.06% |
| 5 | 1–1 (最长) | 4.87% | 0.31% |

### SAGE-L1Tilde-Proj 详细 (7.00%)

| Bucket | MRE |
|:---:|:---:|
| 1 (最短) | 10.02% |
| 2 | 4.65% |
| 3 | 3.30% |
| 4 | 2.39% |
| 5 (最长) | 2.20% |

### GAT-L1Tilde-Proj 详细 (4.96%)

| Bucket | MRE |
|:---:|:---:|
| 1 (最短) | 6.59% |
| 2 | 3.66% |
| 3 | 2.71% |
| 4 | 3.56% |
| 5 (最长) | 4.50% |

---

## 四、分析

### 4.1 RNE：修复爆炸，反超 L1

解耦投影头让 RNE 的 embedding 不需要在同一个线性空间里同时服务对称和非对称两种梯度。`proj_asym` 的小权重初始化让模型先学对称距离（类似 L1），再逐渐启用非对称维度。

**结果**: L̃₁(22.27%) 首次在 RNE 上优于 L1(26.11%)。这证明了解耦投影的策略一有效性。

### 4.2 GAT：持续改善

GAT 的 attention 机制已经提供了隐式方向建模，解耦投影在此基础上进一步优化了 L̃₁ 的维度利用。从 5.49% → 4.96%，接近 0.5pp 的绝对改善。

### 4.3 SAGE：反而变差？

SAGE 的均值聚合缺乏方向先验，解耦投影的额外参数增加了优化负担，但训练不稳（epoch 14 loss 跳变到 0.64）。可能需要：
- 更低的 learning rate
- 更多的 epochs
- 不同的 r/s 比例

### 4.4 策略一有效性总结

| 条件 | 有效？ |
|------|:---:|
| 无结构先验 + 爆炸 (RNE) | ✅ 显著修复 |
| 强方向先验 (GAT) | ✅ 小幅改善 |
| 弱方向先验 (SAGE) | ❌ 稍差 |

解耦投影在**无 Encoder 结构先验**时收益最大——这正是理论预测的场景。

---

## 五、下一步

- [ ] RNE-L1Tilde-Proj 补跑其他城市（Chengdu, Qingdao, Beijing）验证泛化
- [ ] SAGE 调参优化（lr, epochs, r/s 比）
- [ ] 策略二：高非对称子集评估
- [ ] 策略三：方向感知训练

---

## 六、实验记录

| 日期 | 实验 | 模型 | Test MRE | 备注 |
|------|------|------|:---:|------|
| 2026-06-11 | exp1_rne_proj | RNE-L1Tilde-Proj | 22.27% | 🔥 修复爆炸 |
| 2026-06-11 | exp1_sage_proj | SAGE-L1Tilde-Proj | 7.00% | 稍差于硬切片 |
| 2026-06-11 | exp1_gat_proj | GAT-L1Tilde-Proj | 4.96% | ✅ 最佳结果 |
